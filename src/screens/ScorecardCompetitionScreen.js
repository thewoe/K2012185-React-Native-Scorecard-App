import { View, Text } from 'react-native';

const ScorecardCompetitionScreen = () => {
    return (
        <View>
            <Text>Add/Edit Scorecard Competition Screen</Text>
        </View>
    );
};

export default ScorecardCompetitionScreen;
import { View, Text } from 'react-native';

const ScorecardTeamScreen = () => {
    return (
        <View>
            <Text>Add/Edit Scorecard Team Screen</Text>
        </View>
    );
};

export default ScorecardTeamScreen;